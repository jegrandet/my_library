{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <div class="card text-bg-primary mb-3" style="max-width: 18rem;">
        <div class="card-header">Manga</div>
        <div class="card-body">
            <p class="card-title">Nombre de serie {{ nbSerie }}</p>
            <p class="card-title">Nombre de livre {{ nbLivre }}</p>
        </div>
    </div>
{% endblock %}
